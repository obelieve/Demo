<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.zxy.demo">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <!--
                 隐式(implicit)intent，android默认给加上一个CATEGORY_DEFAULT,所以要加个CATEGORY_DEFAULT才能匹配，显式就不用
                -->
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".ExportedService"
            android:exported="true">
            <!--
            实现<intent-filter>标签默认有 android:exported="true"
            -->
            <intent-filter>
                <action android:name="com.zxy.demo.ExportedService"/>
            </intent-filter>
        </service>
    </application>

</manifest>
